# Ultra Clean Home Care

Mobile-first e-commerce app for Ultra Clean, a Nigerian home-care brand.

## Setup
1. Copy `.env.example` to a new file named `.env`
2. Fill in your Supabase URL and anon key (found in Supabase → Settings → API)
3. Run `npm install`
4. Run `npm run dev`
